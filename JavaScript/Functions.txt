Детерминированная функция — функция, которая всегда производит тот же результат при одинаковых вводных данных.

Недетерминированная функция — функция непредсказуемая, её результат зависит от чего-то ещё.

Чистая функция — детерминированная функция, которая не имеет побочных эффектов.
