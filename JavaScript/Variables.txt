var Ч область видимости переменной ограничиваетс€ функцией. »меет hoisting
(при создании переменна€ инициализируетс€ со значение undefined).

let Ч область видимости переменной ограничиваетс€ блоком. Ќе имеет hoisting.
ѕеременную можно переназначить.

const Ч область видимости переменной ограничиваетс€ блоком. Ќе имеет hoisting.
ѕеременную нельз€ переопределить.